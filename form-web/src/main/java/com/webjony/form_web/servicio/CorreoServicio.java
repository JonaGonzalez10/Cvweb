package com.webjony.form_web.servicio;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Service;
import com.webjony.form_web.modelo.ContactoModelo;
@Service
public class CorreoServicio {
    @Autowired
    private JavaMailSender enviarCorreo;

    public void enviarCorreo(ContactoModelo contacto){
        // Implementación de envío de correo
        SimpleMailMessage mensaje = new SimpleMailMessage();
        mensaje.setTo("jony@jonyweb.com");
        mensaje.setSubject("Nuevo Contacto" + contacto.getNombre()); // Asunto
        mensaje.setText("Nombre: " + contacto.getNombre() + "\nCorreo: " + contacto.getCorreo() + "\nMensaje: " + contacto.getMensaje());
        enviarCorreo.send(mensaje);
    }
}
*/